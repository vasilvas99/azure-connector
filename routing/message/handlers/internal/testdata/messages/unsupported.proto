syntax = "proto3";

package protomsg;

message UnsupportedFieldMessage {
	
	// The full text of the message
	string message_id = 1;
	
	// The full text of the message
	string text = 2;
	
	// The message's version
	string version = 3;
	
	// Unsupported JSON field
	reserved unsupported_field = 4;
}
